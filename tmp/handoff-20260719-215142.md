# EDMCModernOverlay fix219 Architecture-Convergence Handoff

Created: 2026-07-19 21:51 PDT

## Current objective

Continue the approved fix219 architecture-convergence implementation one explicitly selected
step at a time. Step 01 is complete. The next logical increment is Step 02, but it has not
been authorized or started in this session.

## Repository state

- Repository: `EDMCModernOverlay`
- Branch: `backend-refactor-implementation`
- HEAD: `be54b2e08abfc55b5b982d28525cf91a393f865d`
- Upstream: `origin/backend-refactor-implementation` at the same commit (0 ahead, 0 behind)
- Worktree: clean
- No pending diff, merge, rebase, or untracked files

## Completed work

### Step 01

Step 01, "Add converged backend models and a shadow control-plane envelope," is complete:

- Added immutable converged identity/status/control-plane models.
- Added deterministic schema-v1 serialization and strict decoding.
- Added bounded histories and privacy-safe diagnostic boundaries.
- Added the developer-only shadow adapter and monotonic shadow producer.
- Preserved transitional status payloads and all production selection/presentation behavior.
- Added representative GNOME, native-X11, XWayland, and unimplemented fixtures.

Authoritative tracking:

- `docs/planning/2026-07-17-fix219-architecture-convergence/implementation/plan.md`
  - Step 1 checklist is checked at line 24.
  - Phase stage 1.1 is `Completed` at line 59.
  - Completion evidence begins at line 180.
- Supporting execution records:
  - `.code-assist/2026-07-17-fix219-architecture-convergence/step01/plan.md`
  - `.code-assist/2026-07-17-fix219-architecture-convergence/step01/progress.md`

### Raster-test and developer-environment follow-up

- Isolated the raster bridge forwarding test from real `/run` filesystem and PyQt font work.
- Confirmed the missing GNOME helper was not the cause of the defective test.
- Standardized the repository-root `.venv` as the developer/test environment.
- Kept `overlay_client/.venv` runtime-only; do not add pytest, ruff, or mypy there.
- Canonical setup is:
  - `python3 -m venv .venv`
  - `source .venv/bin/activate`
  - `python -m pip install -r requirements/dev.txt`
- `make check` and `make test` now prefer `.venv/bin/python` automatically.

Supporting records:

- `.code-assist/2026-07-19-dev-test-environment/raster-test-and-venv/`
- `tests/test_dev_environment_contract.py`

### Task specifications

Nine fix219 task specifications for Steps 01–03 are tracked under:

- `.agents/tasks/2026-07-17-fix219-architecture-convergence/step01/`
- `.agents/tasks/2026-07-17-fix219-architecture-convergence/step02/`
- `.agents/tasks/2026-07-17-fix219-architecture-convergence/step03/`

## Commits after the approved design baseline

- `5aebc2f feat(backend): add shadow control-plane envelope`
- `ce25a95 docs(code-assist): record step 01 implementation`
- `9dfaa1c test(gnome): isolate raster bridge forwarding`
- `fd886ac build(dev): standardize root test environment`
- `be54b2e docs(tasks): add fix219 implementation tasks`

## Fresh validation evidence

An iteration checklist was run after all commits:

- `.venv/bin/python -m pytest overlay_client/tests/test_backend_status.py overlay_client/tests/test_backend_control_plane.py -q`
  - 45 passed.
- `make check`
  - Ruff passed.
  - Mypy passed for 92 source files.
  - GUI-enabled pytest: 1,206 passed, 21 skipped.
- `make test`
  - GUI-enabled pytest: 1,206 passed, 21 skipped.
- `ALLOW_EDMC_PYTHON_MISMATCH=1 .venv/bin/python scripts/check_edmc_python.py`
  - Passed the Python >= 3.10.3 compatibility floor.
  - Warned that local Python 3.12.3 is 64-bit while the preferred EDMC packaging architecture
    is 32-bit.
- `git diff --check`
  - Passed.

Fresh logs are under the ignored directory:

- `.code-assist/2026-07-17-fix219-architecture-convergence/step01/logs/iteration-checklist-targeted.log`
- `.code-assist/2026-07-17-fix219-architecture-convergence/step01/logs/iteration-checklist-make-check.log`
- `.code-assist/2026-07-17-fix219-architecture-convergence/step01/logs/iteration-checklist-make-test.log`

The 21 skips are existing GUI cases gated by their own runtime conditions. No test was
deselected because the GNOME helper is absent. The project has no configured numeric coverage
threshold.

## Decisions and invariants to preserve

- Follow `AGENTS.md` and the approved detailed design before implementation.
- Preserve the `fix219` backend boundary: generic consumers must not import compositor-private
  behavior or dispatch from raw backend/helper identities.
- Implement each plan step as a small, reversible, behavior-scoped increment.
- Use unit tests for pure contracts/models and harness tests whenever `load.py`, plugin hooks,
  lifecycle, settings, server/watchdog, or Tk-facing wiring changes.
- Keep the current production route unchanged until the plan explicitly migrates it.
- Treat support policy, reviewed evidence, and live health as independent axes.
- Keep diagnostics bounded and redact tokens, owner IDs, handles, titles, commands, exceptions,
  and personal paths before serialization.
- Do not implement KDE/KWin or broaden capture/geometry/content/settings scope.

## Pending work and concrete next steps

Step 02 is unchecked and phase stage 1.2 is `Not started`. Its objective is to add behavioral
runtime contracts, generic unavailable/unimplemented runtimes, and a reusable deterministic
paper-backend contract suite without routing production through them.

If the user explicitly authorizes Step 02:

1. Use the code-assist workflow in auto mode and read the complete detailed design plus all
   three Step 02 task files:
   - `step02/task-01-define-behavioral-runtime-contracts.code-task.md`
   - `step02/task-02-add-unavailable-and-unimplemented-runtimes.code-task.md`
   - `step02/task-03-build-paper-backend-contract-suite.code-task.md`
2. Expand/document numbered stages and test selection before editing code.
3. Write the complete unit/contract acceptance surface first and capture RED.
4. Keep the paper backend under test support and out of production registration/imports.
5. Run the required targeted gate:
   - `.venv/bin/python -m pytest overlay_client/tests/test_backend_contracts.py overlay_client/tests/test_backend_runtime_contracts.py -q`
6. Run `make check`, `make test`, and `git diff --check`; record exact outcomes and skips.
7. Update Step 02 and phase stage 1.2 only after its tests, named demo, and evidence are complete.
8. Commit only relevant files with a conventional commit; do not push unless asked.

Step 03 later requires a manual GNOME Shell 46 / Ubuntu 24.04.4 two-monitor performance
baseline. It cannot be marked complete without that environment and evidence, and production
routing must not advance to Step 05 before the baseline gate is satisfied.

## Risks or blockers

- No blocker for Step 02.
- Step 03 has the explicit external-environment/manual-evidence dependency described above.
- Step 01's dated completion note still describes the raster failure present during its
  original run. The later fix and fresh validation logs supersede that historical caveat; it
  is not a current failure.

## Suggested skills

- `code-assist` — required for the next implementation increment and its TDD workflow.
- `handoff` — use to load or resume this document in the next session.
