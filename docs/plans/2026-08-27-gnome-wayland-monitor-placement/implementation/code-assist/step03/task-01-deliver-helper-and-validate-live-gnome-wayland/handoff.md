Status: Manual delivery gate completed by the user; live acceptance matrix pending. The helper was clean-replaced in its expected dedicated UUID directory; the user reported GNOME Wayland (`ubuntu:GNOME`), files installed, extension enabled and ACTIVE, healthy DBus health, protocol 3, and `full_helper` with presentation enabled. The agent invoked no live command or application control.
Files changed: `docs/plans/2026-08-27-gnome-wayland-monitor-placement/implementation/code-assist/step03/task-01-deliver-helper-and-validate-live-gnome-wayland/{context.md,plan.md,progress.md,handoff.md}` and its ignored focused-pytest log; Step 3 status/phase records in `implementation/execution-status.md` and `implementation/plan.md`.
Validation commands/results: `PYQT_TESTS=1 overlay_client/.venv/bin/python -m pytest overlay_client/tests/test_gnome_shell_helper_extension_source.py overlay_client/tests/test_gnome_shell_helper_presentation_state.py overlay_client/tests/test_gnome_helper_presentation_runtime.py overlay_client/tests/test_backend_architecture_boundary.py -q` → 156 passed in 0.37s; `make check` was not rerun because no code changed, with Step 2's root-Ruff and loopback-socket environment limits retained.
Decisions: Test type is manual live GNOME Wayland integration/acceptance with deterministic source-contract/unit regression; no harness because `load.py`/lifecycle wiring is unchanged. Record only non-secret per-case diagnostics and do not repeat configuration paths, timestamps, DBus payloads, or unrelated output.
Risks: Visual placement without matching helper readback is a failure; real Mutter timing, monitor handoff, click-through, focus, stacking, and resize remain unverified. Do not use sleeps, coordinate guesses, fullscreen workarounds, or cross-backend fallbacks if the live matrix fails.
Next exact action: User supplies the five-case live evidence template below, with matching helper readback required for every visually successful case.
