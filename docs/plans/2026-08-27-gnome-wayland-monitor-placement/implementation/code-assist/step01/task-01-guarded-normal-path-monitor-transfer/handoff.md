Status: Blocked only at the required local commit: automated Step 1 implementation, RED → GREEN → REFACTOR, focused validation, scoped review, whitespace validation, and secret scan are complete; Git cannot create `.git/index.lock` because metadata is read-only.
Files changed: `helpers/gnome_shell_extension/extension.js`; `overlay_client/tests/test_gnome_shell_helper_extension_source.py`; approved plan/status/task/code-assist artifacts under `docs/plans/2026-08-27-gnome-wayland-monitor-placement/implementation/`.
Validation commands/results: RED `PYQT_TESTS=1 overlay_client/.venv/bin/python -m pytest overlay_client/tests/test_gnome_shell_helper_extension_source.py -q` → 3 failed, 44 passed as expected; GREEN same command → 47 passed; `git diff --check` → passed; secret scan → no credentials found; default `make check` → blocked before lint because root `python3` lacks Ruff; `PYTHON=overlay_client/.venv/bin/python make check` → 1,639 passed, 21 skipped, 5 unrelated loopback-socket harness setup errors; required `git add ...` → failed once because `.git/index.lock` is read-only.
Decisions: Reused `_normaliseMonitorIndex()`; transfer only for a valid mismatch; matching-frame no-op requires a valid matching monitor; unavailable/error transfer records degradation and continues to resize/readback; diagnostic strategy probes and all excluded backend surfaces remain untouched.
Risks: Live GNOME/Mutter timing, click-through, focus, stacking, and reverse-monitor proof remain manual-only Step 3 work; the broader project gate cannot complete in this sandbox because the harness cannot bind its loopback socket.
Next exact action: User must run `git add helpers/gnome_shell_extension/extension.js overlay_client/tests/test_gnome_shell_helper_extension_source.py docs/plans/2026-08-27-gnome-wayland-monitor-placement/implementation && git commit -m "fix(gnome): transfer overlay before monitor resize"`; then the main thread must reconcile the commit and evidence before generating Step 2.
