# Remediation Plan

## Evidence repair checklist

- [x] Reconcile the approved task, prior handoff, status/plan, scoped diff,
  instruction files, and existing artifacts in a new documentation directory.
- [x] Confirm the appropriate test type remains deterministic unit/source
  contract coverage and that no code correction is warranted before evidence.
- [x] Run and retain full output for the exact focused Step 2 pytest command.
- [x] Run and retain full output for root `make check` and the required
  overlay-client venv alternate.
- [x] Run and retain `git diff --check`; review scope and record the unchanged
  local-commit block without retrying Git writes.
- [x] Update the initial-attempt evidence, dashboard, and phase/stage tables;
  write the exact-six-field remediation handoff.

## Acceptance preservation

1. The focused suite proves optional normal-path diagnostic evidence while
   retaining applied-rectangle authority and the backend boundary.
2. The scoped diff remains test/documentation-only; no helper protocol/schema,
   generic runtime/follow, backend-bundle, X11/XWayland, renderer, or payload
   behavior changes.
3. Root-tooling and sandbox constraints are reported exactly, rather than
   represented as product failures.
