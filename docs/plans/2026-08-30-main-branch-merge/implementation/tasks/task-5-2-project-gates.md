# Task 5.2: Project Gates

Run in a fresh context: backend-boundary pytest, `python scripts/check_edmc_python.py`, `make check`, `make test`, and `git diff --check`. Do not override the Python gate; record exact failures and risks. No merge/content edits, commit, push, reset, or abort. Update plan/dashboard/handoff, then hand off to final integrity review.
