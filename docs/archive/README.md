# Documentation Archive

This directory holds superseded planning, refactoring, and architecture documents.

Policy:

- archive old documents here instead of deleting them
- keep archived documents available for historical context and comparison
- prefer annotating archived documents as superseded rather than removing them

Active documents should remain in their normal locations under `docs/`.
