Status: Completed — centralized annotation-only shared-state contract added; narrow mypy improved from 53 to 5 errors (48 removed), with no runtime/MRO/lifecycle movement.

Files changed: `overlay_client/overlay_state.py`; `overlay_client/interaction_surface.py`; `overlay_client/follow_surface.py`; `overlay_client/control_surface.py`; `overlay_client/overlay_client.py`; `.code-assist/2026-08-23-overlay-client-mypy-debt/stage-2.2-shared-state-contract/context.md`; `plan.md`; `progress.md`; `logs/mypy-shared-state-red.raw.log`; `logs/mypy-shared-state-red.exit-status`; `logs/mypy-shared-state-green.raw.log`; `logs/mypy-shared-state-green.exit-status`; `logs/pytest-offscreen-shared-state-contract.raw.log`; `logs/pytest-offscreen-shared-state-contract.exit-status`; `handoff.md`. No test file changed by this stage.

Validation commands/results: RED once — `source overlay_client/.venv/bin/activate && python -m mypy --follow-imports=skip overlay_client/overlay_client.py overlay_client/interaction_surface.py overlay_client/follow_surface.py overlay_client/control_surface.py`, exit 1 with 53 errors. GREEN once — identical command, exit 1 with 5 errors, a 48-error improvement; all residuals are retained. Offscreen regression once — `source overlay_client/.venv/bin/activate && QT_QPA_PLATFORM=offscreen PYQT_TESTS=1 python -m pytest overlay_client/tests/test_setup_surface.py overlay_client/tests/test_repaint_debounce.py overlay_client/tests/test_follow_surface_mixin.py -q`, passed: 55 passed in 0.99s.

Decisions: The new `OverlayWindowState` `Protocol` contains only setup-owned fields; consumers use `cast("OverlayWindowState", self)` and `TYPE_CHECKING` imports, so there is no runtime contract import or base class. The existing fix219 clear-first paint repair and generic backend boundary were reviewed and preserved. No broad `Any`, ignore, config, test, `load.py`, or lifecycle change was added.

Risks: Five shared-state-family diagnostics remain: two unmodified `_cursor_saved` consumer reads, one `preparation.rect` indexability error, one follow-owned empty-tuple inference error, and one reused local `snapshot` inference error. The latter three are not expressible through the permitted setup-owned attribute contract; all must be reviewed in the next stage. No out-of-family error or runtime/MRO ambiguity appeared.

Next exact action: Coordinator independently reviews the Stage 2.2 scoped diff and this handoff, then updates the top-level dashboard/progress before opening one fresh Stage 2.3 planning context.
