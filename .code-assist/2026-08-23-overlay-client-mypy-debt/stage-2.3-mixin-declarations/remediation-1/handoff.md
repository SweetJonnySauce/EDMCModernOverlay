Status: Completed — remediation attempt 1 removed both remaining Stage 2.3 diagnostics. The focused target now has only the 10 deferred Stage 3.2 renderer diagnostics in `render_surface.py`.

Files changed: `overlay_client/follow_surface.py`; `.code-assist/2026-08-23-overlay-client-mypy-debt/stage-2.3-mixin-declarations/remediation-1/context.md`; `plan.md`; `progress.md`; `handoff.md`; and its raw validation logs. No test, configuration, top-level progress, Qt lifecycle/MRO/timer/paint/focus/follow, backend-selection, or X11-boundary file changed in this remediation.

Validation commands/results: RED once — `source overlay_client/.venv/bin/activate && python -m mypy --follow-imports=skip overlay_client/overlay_client.py overlay_client/interaction_surface.py overlay_client/follow_surface.py overlay_client/control_surface.py overlay_client/render_surface.py`, exit 1 with 12 errors (10 deferred renderer + 2 owned follow-surface errors); GREEN once — identical command, exit 1 with 10 deferred renderer errors only; offscreen once — `source overlay_client/.venv/bin/activate && QT_QPA_PLATFORM=offscreen PYQT_TESTS=1 python -m pytest overlay_client/tests/test_setup_surface.py overlay_client/tests/test_repaint_debounce.py overlay_client/tests/test_follow_surface_mixin.py -q`, exit 0, 55 passed in 0.92s; `git diff --check`, exit 0.

Decisions: The generic consumer callback already enforces `BackendPresentationSurfacePreparation`, with `rect: tuple[int, int, int, int]`; the method now declares that supplied type and directly reads `mode`/`rect`, retaining its existing runtime validation. The device-ratio logging tuple is now the distinct exact `device_ratio_snapshot: tuple[str, float, float, float]`, matching its setup-owned state field. No unchecked/broad `Any` cast, ignore, runtime behavior change, or compositor-specific generic import/dispatch was added.

Risks: The focused target remains nonzero solely because of the 10 known renderer-family diagnostics reserved for Stage 3.2. The wider dirty worktree includes preceding Stage 2.2 and independent fix219 work; this remediation's bounded source delta does not alter the clear-first surface repair or fix219 backend boundary.

Next exact action: Coordinator independently reviews this handoff and bounded diff, then marks Stage 2.3 complete if the scoped review agrees and opens the next approved fresh context for Stage 3.1.
