Status: Incomplete — one focused RED and one focused GREEN were consumed. Exact declaration reconciliation removed all 19 inherited `OverlayWindow` conflicts and three of five owned residuals; two residual diagnostics require a fresh remediation context. The required offscreen regression passed.

Files changed: `overlay_client/setup_surface.py`; `overlay_client/interaction_surface.py`; `overlay_client/follow_surface.py`; `overlay_client/control_surface.py`; `overlay_client/render_surface.py`; `overlay_client/overlay_state.py`; and this Stage 2.3 artifact directory. No test, configuration, `load.py`, Qt MRO, initializer, timer, paint, focus/cursor behavior, or backend-boundary implementation file was changed intentionally.

Validation commands/results: RED once — `source overlay_client/.venv/bin/activate && python -m mypy --follow-imports=skip overlay_client/overlay_client.py overlay_client/interaction_surface.py overlay_client/follow_surface.py overlay_client/control_surface.py overlay_client/render_surface.py`, exit 1, 16 errors (11 renderer + 5 Stage 2.3); GREEN once — identical command, exit 1, 12 errors (11 renderer + 2 Stage 2.3); offscreen regression once — `source overlay_client/.venv/bin/activate && QT_QPA_PLATFORM=offscreen PYQT_TESTS=1 python -m pytest overlay_client/tests/test_setup_surface.py overlay_client/tests/test_repaint_debounce.py overlay_client/tests/test_follow_surface_mixin.py -q`, exit 0, 55 passed in 0.96s; `git diff --check`, exit 0.

Decisions: Added compatible mixin declarations for state that keeps its existing setup owner; refined setup-only annotations to the values actually assigned; used the existing type-only overlay-state cast at the two cursor reads; and typed the preparation-key cache as `tuple[object, ...]`. The 11 focused renderer errors are the Stage 2.1-inventoried Stage 3.2 family and were left untouched. No suppressions, broad new `Any`, runtime bases/storage, compositor-specific generic imports, or raw backend/helper enum dispatch were added.

Risks: `follow_surface.py:388` now requires an exact typed preparation-rect element contract before `int()` conversion; a further unchecked cast would conceal a generic runtime contract. `follow_surface.py:992` reuses `snapshot` after a differently shaped tracker tuple; renaming it to a precise local is behavior-safe but cannot be validated in this context because its sole allowed GREEN check has already run. The shared worktree still contains prior Stage 2.2 and independent fix219 changes, so coordinator review must distinguish them from this stage's declaration-only edits.

Next exact action: Coordinator reviews this incomplete handoff and scoped diff, then opens one fresh Stage 2.3 remediation context to establish the preparation-rect contract and rename the device-ratio snapshot local, run that context's one RED/GREEN cycle, and rerun the required offscreen slice.
