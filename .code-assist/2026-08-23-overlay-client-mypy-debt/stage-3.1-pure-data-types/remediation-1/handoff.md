Status: Completed — all eight source-proven Stage 3.1 annotation diagnostics are removed; the intentionally preserved `payload_model.py:98` TTL coercion diagnostic is the only remaining result from the normal six-module mypy target.

Files changed: `overlay_client/follow_geometry.py`; `overlay_client/plugin_overrides.py`; `overlay_client/transform_helpers.py`; and this remediation context's `context.md`, `plan.md`, `progress.md`, `handoff.md`, and validation logs. No tests, `payload_model.py`, config, `load.py`, CI, Qt/MRO/lifecycle, backend, or fix219/X11 code changed.

Validation commands/results: RED once — `source overlay_client/.venv/bin/activate && python -m mypy overlay_client/follow_geometry.py overlay_client/anchor_helpers.py overlay_client/legacy_processor.py overlay_client/plugin_overrides.py overlay_client/payload_model.py overlay_client/transform_helpers.py`, exit 1 with 9 errors in 4 files (eight owned annotations plus TTL); GREEN once — identical normal-import command, exit 1 with only `payload_model.py:98`; pure-unit slice — `source overlay_client/.venv/bin/activate && QT_QPA_PLATFORM=offscreen PYQT_TESTS=1 python -m pytest overlay_client/tests/test_follow_geometry.py overlay_client/tests/test_anchor_helpers.py overlay_client/tests/test_transform_helpers.py overlay_client/tests/test_payload_dedupe.py overlay_client/tests/test_override_grouping.py -q`, exit 0, 90 passed in 0.37s; `source overlay_client/.venv/bin/activate && python -m ruff check overlay_client/follow_geometry.py overlay_client/plugin_overrides.py overlay_client/transform_helpers.py`, exit 0; `git diff --check`, exit 0.

Decisions: Declared the clamp-native origins at their shared pre-branch seam as `float`, retained all assignments and calculations, applied the existing `Tuple[PrefixEntry, ...]` contract, and materialized existing two-coordinate points as exact pairs. No new test was added because all changes are annotation-only and existing focused unit tests pass. The TTL error has neither a cast, guard, suppression, nor input-contract change.

Risks: The normal scoped target remains intentionally non-green because `PayloadModel.ingest` accepts `dict[str, object]` while directly passing TTL to `int()`. Resolving it needs a separate, reviewed runtime input-contract decision and test-first behavior proof. Directory-wide client type debt and the independent X11 manual validation remain outside this context.

Next exact action: Coordinator review this handoff and bounded diff, mark Stage 3.1 complete if the TTL diagnostic remains intentionally deferred, then open one fresh Stage 3.2 renderer context; do not modify TTL behavior without user-approved contract work.
