Status: Incomplete — the required one RED/GREEN cycle improved the focused six-module result from 16 to 5 errors and the specified pure-unit slice passed. The direct TTL coercion diagnostic is preserved as an explicit coordinator stop; four source-proven clamp-geometry diagnostics require a fresh context because this context's only GREEN measurement is consumed.

Files changed: `overlay_client/follow_geometry.py`; `overlay_client/anchor_helpers.py`; `overlay_client/legacy_processor.py`; `.code-assist/2026-08-23-overlay-client-mypy-debt/stage-3.1-pure-data-types/context.md`; `plan.md`; `progress.md`; `handoff.md`; and its raw validation logs. No test, configuration, `load.py`, payload-model, plugin-override, transform-helper, Qt/MRO/lifecycle/follow, renderer, backend, compositor, or fix219/X11 source file changed.

Validation commands/results: RED once — `source overlay_client/.venv/bin/activate && python -m mypy --follow-imports=skip overlay_client/follow_geometry.py overlay_client/anchor_helpers.py overlay_client/legacy_processor.py overlay_client/plugin_overrides.py overlay_client/payload_model.py overlay_client/transform_helpers.py`, exit 1 with 16 errors in 4 files; GREEN once — identical command, exit 1 with 5 errors in 2 files (11-error improvement: four clamp-geometry and one TTL remain); pure-unit slice once — `source overlay_client/.venv/bin/activate && python -m pytest overlay_client/tests/test_follow_geometry.py overlay_client/tests/test_anchor_helpers.py overlay_client/tests/test_transform_helpers.py overlay_client/tests/test_payload_dedupe.py overlay_client/tests/test_override_grouping.py -q`, exit 0, 90 passed in 0.32s; `git diff --check`, exit 0.

Decisions: Applied only source-proven annotations: float native origins in the standard geometry converter and later clamp paths; `float | str` trace-detail values; mapping-compatible copied transforms; and `int | str` legacy vector point dictionaries. The current worktree already has no focused `plugin_overrides.py` or `transform_helpers.py` diagnostic, so those files were left unchanged. `PayloadModel.ingest` continues to accept `dict[str, object]` and directly passes TTL to `int()`; source/call sites do not prove a closed static representation for every existing accepted runtime coercion, so no guard, narrowing, broad cast, ignore, or behavior change was introduced.

Risks: `follow_geometry.py:185`, `:187`, `:202`, and `:204` remain because the clamp function first assigns integer branch values to the shared locals before the later branch-local annotations; a fresh context can establish `float` at that common declaration seam and remeasure. `payload_model.py:98` remains an explicit stop: resolving it safely requires coordinator review of the input contract or a behavior-changing, test-first decision. The wider worktree is intentionally dirty; this bounded diff does not alter the independent clear-first repair or fix219 backend boundary.

Next exact action: Coordinator independently reviews this incomplete handoff and bounded diff; preserve the TTL diagnostic as stopped, then open one fresh Stage 3.1 remediation context to move the clamp-origin declaration to the common earlier assignment seam, run that context's one RED/GREEN cycle and pure-unit slice, and stop again if the TTL contract remains unproven.
